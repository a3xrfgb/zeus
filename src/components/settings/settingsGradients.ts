/**
 * Settings selection accents — solid colors per theme via `index.css`
 * (`.settings-toggle-on`, `.settings-nav-active-fill`).
 */

/** @deprecated Use `settingsToggleOnClassName`. */
export const settingsToggleOnLightBg = "settings-toggle-on";

/** @deprecated Use `settingsToggleOnClassName`. */
export const settingsToggleGradientBg = "";

/** Active settings nav `before:` layer — see `.settings-nav-active-fill` in index.css. */
export const settingsNavActiveBeforeGradient = "settings-nav-active-fill";

/** Switches / toggles when checked. */
export const settingsToggleOnClassName = "settings-toggle-on";
